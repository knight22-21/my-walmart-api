# render.yaml
services:
  - type: web
    name: walmart-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn fastapi_server:app --host 0.0.0.0 --port 10000
